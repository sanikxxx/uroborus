# Digital Ruble Agent - MVP
# Зависимости

# Основные фреймворки
openai>=1.0.0  # Для интеграции с LLM (поддержка OpenAI-compatible API)
ollama>=0.1.0  # Для работы с Ollama локально
python-dotenv>=1.0.0  # Для управления переменными окружения

# Для RAG (поиска по релевантным документам)
chromadb>=0.4.0  # Для RAG-поиска (векторная БД)
sentence-transformers>=2.0.0  # Для эмбеддингов

# Для работы с API
requests>=2.31.0  # Для HTTP-запросов к API логов
uvicorn>=0.24.0  # Для создания API сервера (опционально)
fastapi>=0.104.0  # Для создания API сервера (опционально)

# Для веб-интерфейса (если будет UI)
gradio>=4.0.0  # Для простого веб-интерфейса (опционально)

# Прочие утилиты
pydantic>=2.0.0  # Для валидации данных
